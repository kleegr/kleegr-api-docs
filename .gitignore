dist/
node_modules/
.env
.DS_Store
*.log
.astro/
# Spec files are fetched at build time by scripts/fetch-specs.mjs
# Committed: small utility specs (workflows, campaigns, businesses, surveys, snapshots, courses)
# Fetched at build: contacts, conversations, calendars, opportunities, invoices, payments, users, locations, forms, medias, funnels, blogs, social-media-posting
